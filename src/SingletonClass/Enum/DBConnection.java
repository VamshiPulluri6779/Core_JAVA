package SingletonClass.Enum;

/*
    * Enum Singleton is a thread-safe singleton implementation using Java Enum.
    * It is the most effective way to create a singleton in Java.
    * By default, constructor in enum is private
 */
public enum DBConnection {

    INSTANCE;
}
